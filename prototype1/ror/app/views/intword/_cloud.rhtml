     <% intwords.sort_by { |w| w[:name] }.each do |w| %>
      <%= content_tag('a', w.name, :style =>
        font_size_for_tag_cloud_new( w.intword_statistic.attributes[@attr],
          intwords.last.intword_statistic.attributes[@attr],
          intwords.first.intword_statistic.attributes[@attr]),
          :href => ["../intword/show/", w.id].join ) %>
     <% end %>
